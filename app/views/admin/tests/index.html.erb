<h1><%= t('.header') %></h1>

<table>
  <tr>
    <th><%= Test.human_attribute_name(:title) %></th>
    <th><%= Test.human_attribute_name(:questions) %></th>
    <th colspan="3"><%= t('actions') %></th>
  </tr>

  <%= render @tests %>
</table>

<%= link_to t('create_m', model: Test.model_name.human), new_admin_test_path %>
