<h1><%= @test.title %></h1>

<table>
  <tr>
    <th>Questions</th>
    <th colspan="3">Actions</th>
  </tr>

  <%= render @test.questions %>

</table>

<div><%= link_to 'New question', new_test_question_path(@test)  %></div>
<div><%= link_to 'Back to tests', tests_path %></div>
